import activityRepository from '@/repositories/activity-repository';

// describe ('get activities', () => {
//     it('Should return the activity for the given userId', async () => {
//         const userId = 1;
//         jest.spyOn(activityRepository, '')
//     } )
// })
